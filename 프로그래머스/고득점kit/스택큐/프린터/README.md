## input && output
```
priorities : 
[2,1,3,2]

location : 인덱스 2짜리 문서는 몇번째로 인쇄되는가?
2

return
1
```

## notes
```
1. 디큐
2-1. 가장 중요한 문서라면 : 인쇄
2-2. 그렇지 않다면 : 인큐 
```

## strategy
```
정직하게 덱 써서 하면 될것같긴 하다.
다만 그때그때의 최댓값을 알고 있어야 하므로 힙 또한 사용하면 좋을듯.

1. 주어진 priorities로 최대힙과 큐를 만든다. 큐를 만들땐 enuemrate 해줘서 원래 인덱스를 저장해준다.
2. 다음을 반복한다 ()
    힙에서 하나 꺼낸다.
    다음을 반복한다
        큐에서 하나 꺼낸다.
        힙과 같은 값인가? print_count 하나 올려주고, 만약 location의 값이었다면 print_count 리턴
        그렇지 않다면? 인큐
    
```
